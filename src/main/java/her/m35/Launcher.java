package her.m35;

import javafx.application.Application;

/** Launcher class which acts as the entry point to the HERM35 application. */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
